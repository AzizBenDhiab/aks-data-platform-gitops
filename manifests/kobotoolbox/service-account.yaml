apiVersion: v1
kind: ServiceAccount
metadata:
  name: kobotoolbox-workload-identity
  namespace: kobotoolbox
  annotations:
    # Note: The client-id will be set by the deployment pipeline via kubectl patch
    # This ensures the ServiceAccount gets the correct client ID at deployment time